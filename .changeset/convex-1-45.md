---
"@confect/cli": patch
"@confect/react": patch
"@confect/server": patch
---

Build and test against `convex` 1.45.0 and `convex-test` 0.0.56. Peer ranges are unchanged, so no consumer action is required.
