name: Docs

on:
  push:
    branches:
      - main
      - v9
  pull_request:
    branches:
      - main
      - v9

jobs:
  format:
    name: Format
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: ./.github/actions/docs-setup
      - run: pnpm format

  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: ./.github/actions/docs-setup
      - run: pnpm lint

  broken-links:
    name: Broken Links
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: ./.github/actions/docs-setup
      - run: pnpm --filter docs exec mintlify broken-links

  validate:
    name: Validate
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: ./.github/actions/docs-setup
      - run: pnpm --filter docs exec mintlify validate

  a11y:
    name: Accessibility
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: ./.github/actions/docs-setup
      - run: pnpm --filter docs exec mintlify a11y
