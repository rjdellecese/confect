---
"@confect/server": patch
"@confect/core": patch
"@confect/cli": patch
---

`confect/_generated/refs.ts` now default exports the `Refs` object, which now contains `public` and `internal` fields for each corresponding collection of Confect functions
